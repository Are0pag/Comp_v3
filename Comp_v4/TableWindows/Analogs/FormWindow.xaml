<Window x:Class="Comp_v4.TableWindows.Analogs.FormWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:local="clr-namespace:Comp_v4.TableWindows.Analogs"
        mc:Ignorable="d"
        Title="Форма взаимозаменяемости" Height="300" Width="500"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">
    
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>
        
        <!-- Основной компонент -->
        <TextBlock Grid.Row="0" Grid.Column="0" 
                   Text="Основной компонент:" 
                   VerticalAlignment="Center" 
                   Margin="0,0,10,10"/>
        
        <TextBox Grid.Row="0" Grid.Column="1" 
                 Grid.ColumnSpan="2"
                 Text="{Binding SourceComponent.Name, Mode=OneWay}" 
                 IsReadOnly="True"
                 Margin="0,0,0,10"
                 Padding="5"/>
        
        <!-- Аналог -->
        <TextBlock Grid.Row="1" Grid.Column="0" 
                   Text="Аналог:" 
                   VerticalAlignment="Center" 
                   Margin="0,0,10,10"/>
        
        <TextBox Grid.Row="1" Grid.Column="1" 
                 Text="{Binding RelatedComponent.Name, Mode=OneWay}" 
                 IsReadOnly="True"
                 Margin="0,0,5,10"
                 Padding="5"/>
        
        <Button Grid.Row="1" Grid.Column="2" 
                x:Name="SelectAnalogButton"
                Content="Выбрать..." 
                Command="{Binding SelectAnalogCommand}"
                Margin="0,0,0,10"
                Padding="10,5"
                MinWidth="80"/>
        
        <!-- Чекбокс -->
        <CheckBox Grid.Row="2" Grid.Column="0" 
                  Grid.ColumnSpan="3"
                  Content="Всё количество"
                  IsChecked="{Binding IsAllCount}"
                  Margin="0,0,0,20"
                  VerticalAlignment="Center"/>
        
        <!-- Панель кнопок -->
        <Border Grid.Row="4" Grid.Column="0" Grid.ColumnSpan="3"
                Background="{DynamicResource {x:Static SystemColors.ControlBrushKey}}"
                Padding="10"
                BorderThickness="0,1,0,0"
                BorderBrush="Gray">
            
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="Сохранить" 
                        x:Name="SaveButton"
                        Command="{Binding ClickCommand}"
                        Margin="5,0"
                        Padding="15,5"
                        MinWidth="80"
                        IsDefault="True"/>
                
                <Button Content="Отмена" 
                        Command="{Binding CancelCommand}"
                        Margin="5,0"
                        Padding="15,5"
                        MinWidth="80"
                        IsCancel="True"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>